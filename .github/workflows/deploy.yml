name: Deploy Widgets to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      
      - name: Install root dependencies
        run: npm install fs-extra
      
      - name: Build Geo Map Widget
        working-directory: packages/geo-map
        run: |
          npm install
          npm run build

      - name: Build Smart GIS Widget
        working-directory: packages/smart-gis
        run: |
          npm install
          npm run build

      - name: Build Smart GIS v2 Widget
        working-directory: packages/smart-gis-v2
        run: |
          npm install
          npm run build

      - name: Build Scrollytelling Widget
        working-directory: packages/scrollytelling
        run: |
          npm install
          npm run build

      - name: Build Territoire 3D Widget
        working-directory: packages/territoire-3d
        run: |
          npm install
          npm run build

      - name: Build Territoire 3D Component Widget
        working-directory: packages/territoire-3d-component
        run: |
          npm install
          npm run build

      - name: Build Smart Map 3D Widget
        working-directory: packages/smart-map-3d
        run: |
          npm install
          npm run build

      - name: Prepare dist folder
        env:
          BASE_URL: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}
        run: npm run prepare-dist
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist
      
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
